1. 现在有这么一个需求，我需要记录每个时刻的方块信息，并且能够获取所有时刻的某一方块的信息，同时我也需要能获得同意时刻的所有方块信息，利用java如何实现
2. 现在有这样一个需求，每个方块在每个游戏刻会受到多次更新，其中更新是有顺序的，需要一个类UpdateInfo，用于记录更新的类型，每个游戏刻更新的次序

3. 在java中，现在有这样一个需求，有一个有一组元数据（UpdateInfo），其中每个元数据（UpdateInfo）中的的时刻gameTick、方块位置BlockPos、和次序order可以确定唯一的一个元数据，我现在需要根据gameTick、BlockPos进行分类访问，请问使用什么方法实现比较好
3.1 那如果我需要获取某一gameTick的所有元数据，也要能够获得某一方块的所有元数据，怎么做